pipeline {
    agent any

    stages {
        stage('Bake image') {
            steps {
                sh'sudo packer init docker-image.pkr.hcl'
                sh'sudo packer build docker-image.pkr.hcl'
            }
        }
    }
}